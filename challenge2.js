import { orderJson } from './data.js'

// Restructure the "items" of orderJson to a variable called unique_ptg_part_number

// unique_ptg_part_number should be an array of strings, the ptg_part_number.

// Remove any duplicates which exist.
// Hint: look up a JS Set

// This is an example of what unique_ptg_part_number should look like
// ["KK1609","SWLG01_CL", ...etc]


console.log(unique_ptg_part_number)