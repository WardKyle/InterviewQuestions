import { orderJson } from './data.js'

// Make a deep copy of orderJson into a variable called deepCopy, and remove any fields which are falsy

// Hint: Shallow clone != deep clone


console.log(deepCopy)